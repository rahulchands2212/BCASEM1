Array :- array is the collection of homogeneous data type in a contiguous memory allocation.
we can access the element with the help of index;

syntax :- 
data_type name [size];
int arr[10];

example:-

sum of 2 number ;
#include<stdio.h>
int main(){
    int sum[2]; //define array;

//insert element;
printf("enter number a and b :\n");
    //take input number in array;
    for(int i=0;i<2;i++){
        scanf("%d",&sum[i]);
    }

//traverse;
int sums =0;
for(int i=0;i<2;i++){
    sums += sum[i];
}

//print sum;
printf("sum = %d",sums);

return 0;
}