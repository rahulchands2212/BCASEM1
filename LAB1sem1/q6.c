#include<stdio.h>
int main(){
     int length = 1189,breadth = 841,c=0;
     printf("A0 = %dmm x %dmm\n",length,breadth);

     // A1
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A1 = %dmm x %dmm\n",length,breadth);

      // A2
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A2 = %dmm x %dmm\n",length,breadth);

     // A3
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A3 = %dmm x %dmm\n",length,breadth);

     // A4
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A4 = %dmm x %dmm\n",length,breadth);

     // A5
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A5 = %dmm x %dmm\n",length,breadth);

     // A6
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A6 = %dmm x %dmm\n",length,breadth);

     // A7
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A7 = %dmm x %dmm\n",length,breadth);

     // A8
     c = length;
     length = breadth;
     breadth = c/2;
     printf("A8 = %dmm x %dmm\n",length,breadth);
    
   return 0;
}
